#include "main.h"

/**
 * add - sum function
 * dscription: function that adds two integers and returns the result.
 * @m: m parameter
 * @n: n parameter
 *
 * Return: add
 */
int add(int m, int n)
{
	return (m + n);
}
